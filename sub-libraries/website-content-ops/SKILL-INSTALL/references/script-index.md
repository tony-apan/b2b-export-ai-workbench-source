---
doc_id: allincms-bulk-script-index
title: AllinCMS 脚本索引
description: allincms-bulk-content-upload skill 全部辅助脚本的用途索引(从 SKILL.md 外移);手工调用命令在 SKILL.md Workflow 各步
layer: ops
status: "Working"
created: 2026-07-04
updated: 2026-07-30
page_type: index
sources: ["self"]
confidence: medium
last_updated: 2026-08-31
visibility: "public"
redaction_status: "safe-to-publish"
related: ["../README.md"]
owner: "AI"
type: "doc"
---

# 脚本索引

## Active portable runtime maintenance

- `scripts/resolve_website_content_ops_root.py`: prefer an exact/environment/discovered full canonical checkout, then verify the installed bundled runtime manifest, exact immutable file set, every SHA-256, internal symlink absence and dependency-ready marker. It rejects invalid high-priority roots, `dist/`, incomplete source checkouts and tampered bundles; `runtimeBundleValidated` and `controllerExecutable` are deliberately separate results.
- `scripts/build_bundled_runtime.py`: maintainer-only generator that reads an explicit allowlist from exact committed canonical Git bytes and atomically rebuilds `vendor/website-content-ops-runtime`; it never executes CMS operations.

`scripts/` 下辅助脚本的用途索引(每条脚本自己也有 `--help`)。按用途查"哪个脚本做什么";
实际手工调用的完整命令在 `SKILL.md` 的 Workflow 各编号步骤里。运行时/门禁脚本、校验器、
handoff/brief 构造器都在此列全。

- `scripts/make_create_preflight_evidence.py`: generate a pure read-only create-site preflight evidence JSON from observed site keys before submitting the create form.
- `scripts/make_create_preflight_from_existing_site_evidence.py`: derive a `create_preflight_verified` create-site preflight file from fresh `existing_site_selected` read-only evidence after a real `/sites` refresh. Use it when the browser refresh already captured site keys and scoped create-dialog fields, but the next stage is `create_site_submit`.
- `scripts/make_existing_site_readonly_evidence.py`: generate read-only evidence after selecting and inspecting an existing site without submitting, saving, uploading, publishing, or deleting.
- `scripts/redact_browser_scan.py`: redact raw browser scan JSON before storage, scan-to-evidence conversion, or handoff; removes account/menu noise, emails, frontend domain labels, and long raw text while preserving backend module URLs and neutral controls.
- `scripts/make_existing_site_evidence_from_scan.py`: convert a redacted browser scan JSON into existing-site read-only evidence to avoid hand-copying long refresh commands. It accepts protected module URLs with `{siteKey}` or `{realSiteKey}` placeholders after redaction and reconstructs concrete module routes from the verified `siteKey`. For non-table setup/grid pages such as media, site-info, tracking, and domains, it can use visible controls/inputs as the content inspection proof instead of requiring table headers.
- `scripts/make_created_site_evidence.py`: upgrade preflight evidence after a real create-site submit and current new-site verification; optionally merge generated frontend rendering and launch readiness evidence into the same run-evidence file.
- `scripts/merge_created_site_readonly_refresh.py`: merge current read-only setup-page evidence into older `created_verified` site evidence when validators add required setup modules or a fresh read-only refresh fills missing setup keys. It preserves the original create timestamp and authorization, so use it only as phase evidence; it does not make stale evidence fresh for mutation.
- `scripts/make_authorization_record.py`: generate an action-specific mutation authorization record; use before create-site submit, draft creation, existing product/post save or publish, probe save/publish/cleanup, upload, cleanup, or batch operations.
- `scripts/check_pre_mutation_gate.py`: verify that preflight/read-only evidence and action-specific authorization match before create-site submit, content-type-specific probe creation, existing product/post save or publish, batch operations, media upload, and site/theme/form/domain/tracking actions.
- `scripts/simulate_full_e2e_chain.py`: run the full local-only dry run by orchestrating site creation, static launch evidence, module scan summary, staged capture plan, content probe lifecycle, summaries, and closeout in one command.
- `scripts/simulate_site_creation_chain.py`: run a local-only dry run of preflight, authorization, final gate, created-site evidence, run summary, closeout gate, and validation before real browser submit.
- `scripts/simulate_probe_lifecycle.py`: run a local-only staged dry run from created/read-only evidence through probe creation, request capture, sample verification, cleanup, final summary, and closeout.
- `scripts/validate_manifest.py`: run before posts/products batch upload when the source content is represented as JSON. Use `--json` for machine-readable validation in rehearsals or parallel checks. Media, themes, routes, and forms need their own schema-specific validator after request capture.
- `scripts/validate_slate_content_shape.py`: run on a products/posts manifest before it enters the batch to assert every `content` field is a Slate node array (`[{type,children:[{text}],id?}]`), not a markdown/HTML string. A UI form save does not bind Slate, so a string body persists empty on the live frontend; this gate catches that silent-empty-body failure locally. Use it right after the AI content pass and before sample/batch upload.
- `scripts/check_next_action_freshness.py`: run right before a JSON batch to refuse replay when the captured save contract's `deploymentId` does not match the current live deployment, when the deployment marker is missing, or when required next-action ids are absent. `next-action` ids change per deployment and a stale id fails silently, so read the current deployment id live and pass `--current-deployment-id` before the batch.
- `scripts/check_content_quality.py`: pre-publish CONTENT-quality gate (complements the structural `validate_source_site_package.py`). Scans `contentPlan.products[*]`/`posts[*]` and BLOCKS on stock/placeholder images (in `coverImage`/`media` object-or-array/`gallery`/top-level `contentPlan.media`/content body), missing alt on a main product image, and unreplaced placeholder/template markers; WARNS on off-category/leftover-demo terms, a thin spec table (<3), duplicate copy across items, gallery images without alt, and `example.com` references; and returns an `aiReviewChecklist` for judgments that can't be automated (hallucinated specs, unbacked differentiators, fabricated trust claims). `--strict` promotes warnings to blockers. Run before a content batch/publish.
- `scripts/build_content_skeleton.py`: generation-time AUTHORING scaffold (the piece that makes format compliance correct-by-construction instead of caught-after). Given a product/post's structured source fields, emits a `content-format-standard.md` Slate body — hook + bolded key fact, H2 sections, bullet lists, CTA — so the AI FILLS real leaf text into a correct structure rather than free-writing a wall. `--apply` writes skeletons into items whose body is thin/missing; reports `needs` per item (missing source fields, `ready=false`) and never fabricates (`[fill: ...]` markers). Its output passes `check_content_format` by construction (cross-checked in `test_build_content_skeleton.py`). Run BEFORE the format gate.
- `scripts/check_content_format.py`: pre-publish CONTENT-FORMAT gate (complements `check_content_quality.py`). Reads each product/post Slate body and flags structure gaps per `content-format-standard.md`: a long body with zero headings/lists/bold = paragraph-WALL block; <2 section headings / no bold / no list / no CTA / an over-long paragraph = warn; returns a formatChecklist. `--strict` promotes warnings. Run before a content batch/publish.
- `scripts/check_template_residue.py`: whole-site TEMPLATE-RESIDUE gate (complements `check_content_quality.py`). Feed it the OLD default-template fingerprint blacklist captured BEFORE conversion (old brand / product / category / tag / email / phone / address / domain / author / CTA) + the visible text of EVERY live route (home + each product/post/page + header/footer/nav/meta). It scans every route for every term; any hit = BLOCK with route+term; zero across all routes is the launch condition. Turns `batch-verification.md`'s "scan every route for residue" prose into an enforced gate. Run before declaring a converted-from-template site launch-ready.
- `scripts/plan_residue_fixes.py`: turn a residue report into a per-TYPE fix work-order. Routes each `check_template_residue` hit to its fix target — taxonomy chip → backend category/tag tab (not the designer); product/post detail → the item's JSON save (product spec rows included); global block (footer/nav/contact modal/newsletter) → the exact owning designer layer; standalone page → theme page designer; homepage → the owning module — each group carrying the fix method + re-verify step, so the AI works leftovers by type without editing the wrong layer. Read-only. Loop: fix a group → re-verify its routes → re-run the whole-site residue gate to zero.
- `scripts/resolve_run_mode.py`: resolve the RUN MODE and which gates apply. `created_verified` (new site) → `from_scratch` (residue gate required, no prompt); `existing_site_selected` → ambiguous, so it defaults to `template_conversion` (residue gate ON) and sets `needsUserConfirmation` — the AI must ASK whether it's a from-template conversion (scan whole site) or a daily `incremental_update` to an already-clean site (skip residue scan). `incremental_update` is never a silent default; only an explicit `--declared-mode` (the user's answer) selects it. Content-quality gate applies in every mode. Biases ambiguity toward scanning.
- `scripts/validate_theme_page_document.py`: validate a theme design-save JSON replay body (`[{siteId,themeId,pageId,intent:'save',pageDocument:{root,elements:{id:{type,props}}}}]`) before sending it. Use `--require-publication-ready` to also reject placeholder copy and local/relative image paths in block props. This is the local half of theme-JSON replay: once a live CDP capture supplies the current design-save `next-action` id, the whole-page replay body is validated by this gate (see `references/server-action-save-api.md` §7).
- `scripts/make_source_input_requirements.py`: generate a run-evidence JSON that lists which user/PDF/catalog/website/spreadsheet fields are required, recommended, source-derived, user-confirmed, or blocked until schema capture. Use it before extracting source materials into posts/products/forms/media/theme-page/site-info/routes/domains/tracking/navigation manifests or runbooks.
- `scripts/record_source_input_gap.py`: append per-operation field gaps into a local run ledger outside the skill package. Use it while operating the browser whenever a field is discovered that later needs user input, PDF/catalog extraction, schema capture, media proof, or an explicit omit/acceptance decision. Feed that ledger back into `make_source_input_requirements.py --gap-ledger` before extracting user PDFs/catalogs/websites/spreadsheets so the source-intake contract includes browser-discovered gaps.
  Run it again with `--validate-only` before using an older or merged ledger; this checks that every gap row still has source hints, generation rules, evidence pointers, proof notes when needed, no sensitive strings, no duplicates, and a matching summary.
- `scripts/make_resolved_source_input_gaps.py`: create a separate `allincms_resolved_source_input_gaps` evidence file when later browser proof supersedes an older operation gap. Use this instead of editing or deleting old ledger rows. Pass the generated file to `make_source_input_requirements.py --resolved-gap-evidence`, `prepare_source_site_package.py --resolved-gap-evidence`, or `run_source_file_rehearsal.py --resolved-gap-evidence` so stale blockers are filtered while the original gap audit trail remains intact.
- `scripts/make_manifest_upload_readiness.py`: build a multi-manifest readiness report after draft validation. Use it when products and posts are prepared together so `blocked` schema gates are visible and generic validation is not mistaken for upload readiness.
- `scripts/build_source_inventory.py`: create a local source inventory from user files/directories before raw extraction, wiki distillation, or package generation.
- `scripts/init_workspace.py`: scaffold a PRIVATE multi-client/multi-site workspace OUTSIDE the skill (client PII must never enter the public repo). `--action init-workspace|new-client|new-site`. Creates the Karpathy layout (append-only `raw/`, distilled client `wiki/`, per-site `sites/<site>/` with source-wiki/package/run/residue-blacklist/live) and indexes each site in the workspace README. NEVER overwrites existing client/site materials; refuses to scaffold inside the skill package; slugs are kebab-case. See `references/workspace-layout.md`.
- `scripts/site_build_status.py`: report a workspace site's build-pipeline progress + next step + gate still owed. Reads the site folder's `run/` markers (source-wiki/package present; content-quality-report / run-mode / site-live / residue-report / launch-acceptance JSON) and returns ordered done-stages, the nextStage with a command hint, and gatesPending. Read-only, never runs the build. Resumes a client's multiple sites; source of truth for the workspace README status column. See `references/build-pipeline.md`.
- `scripts/extract_source_materials.py`: extract inventory files into local raw-extraction artifacts and a conservative extraction summary for the source-wiki builder. Use it for PDFs, DOCX, spreadsheets, CSV/TSV, Markdown, TXT, JSON, HTML, and image metadata.
- `scripts/build_source_wiki.py`: build a local `allincms_source_wiki` JSON from a source inventory and optional extraction summary.
- `scripts/validate_source_wiki.py`: validate the source wiki against the inventory, source refs, required pages/products/posts/site fields, and leakage/raw-Markdown checks.
- `scripts/export_source_wiki_markdown.py`: export `allincms_source_wiki` into local readable Markdown wiki files (`index.md`, `site.md`, `pages.md`, `products.md`, `posts.md`) for user/operator review. These files are review artifacts, not upload payloads.
- `scripts/upload_media_via_picgo.py`: **external-host fallback, not the default AllinCMS media route**. It uploads local source images referenced by an `allincms_source_wiki` through a PicGo local server, rewrites those references to returned hosted URLs, and writes an `allincms_media_upload_map`. Use it only when the target is intentionally an external image host or the user explicitly chooses PicGo. For the AllinCMS media library, open the exact logged-in media page and use the canonical adapter in `<SUB_LIBRARY_ROOT>/ADAPTERS/cms/allincms/AI-START-HERE.md`. PicGo is not an AllinCMS mutation (`allincmsRemoteMutationsPerformed=false`) but is still a real public upload, so it defaults to `--dry-run` and requires `--confirm-upload`; never store image bytes, PicGo config, or tokens in the skill package.
- `scripts/make_source_wiki_refinement_plan.py`: convert source-wiki/package/review validation issues into field-level source-wiki repair actions. Use it when source packages are not review-ready so the next AI pass knows which page/product/post/site-info/media/contact/taxonomy fields to rewrite, source, confirm, or defer.
- `scripts/make_source_wiki_refinement_brief.py`: turn the source wiki, refinement plan, and optional Markdown wiki exports into a safe AI/operator brief for rewriting `allincms_source_wiki`. Use it after `needs_source_wiki_refinement` so the next pass has explicit targets, output contract, validation command, and remote-mutation prohibitions.
- `scripts/draft_refined_source_wiki.py`: generate a local refined source-wiki draft from the current source wiki and refinement brief when extracted products/posts are usable but pages/site-info/navigation/policies need deterministic expansion before review. It can expand thin pages, add homepage/navigation/policy defaults, and validate the same output contract; it does not replace user confirmation or live schema capture.
- `scripts/prepare_source_site_package.py`: run the local source-file preparation chain in one command: inventory, raw extraction, source wiki JSON, readable Markdown wiki, source-input requirements, source-site package, optional review packet, and source execution status. It never performs remote mutation; if the output is not `review_ready`, refine the source wiki/package before asking for confirmation.
- `scripts/apply_refined_source_wiki.py`: after AI/user refinement turns `source-wiki.json` into a publication-ready `allincms_source_wiki`, export readable Markdown wiki files, rebuild and validate the source-site package, generate the review packet, and refresh source execution status in one local-only step. Use it to move `needs_source_wiki_refinement` runs toward user confirmation without hand-wiring package/review/status commands.
- `scripts/validate_refined_source_wiki_contract.py`: validate a refined `allincms_source_wiki` against the refinement brief that requested it. Use it after AI rewrites source wiki content or before applying a copied refined wiki; it checks output path binding, source refs, non-authorization flags, placeholder removal, navigation/media/contact/taxonomy presence, and optional inventory validation.
- `scripts/run_source_file_rehearsal.py`: local-only orchestration for real "user gave files" runs. It calls initial source preparation, can auto-draft a refined source wiki with `--auto-draft-refined-source-wiki`, optionally applies a refined source wiki, and optionally prepares confirmed execution artifacts when user confirmation text is supplied. Use it to prove whether the run is blocked at source-wiki refinement, ready for content confirmation, or ready for browser create/select-site work. It does not create sites, save content, upload, publish, or authorize mutations.
- `scripts/validate_source_file_rehearsal.py`: validate `source-file-rehearsal-summary.json` before showing a confirmation brief, reusing an older rehearsal, or continuing to browser work. It reopens package/review/brief artifacts, checks objective-audit and stage drift, and rejects sensitive raw header/account text.
- `scripts/make_source_confirmation_brief.py`: convert `source-file-rehearsal-summary.json` into compact `source-confirmation-brief.json` and optional Markdown for user-facing content confirmation. It merges `confirmationReview` and `objectiveAudit`, exposes counts, coverage, suggested confirmation text, deferrals, command templates, and the next blocker, while keeping `isRemoteMutationAuthorization=false`.
- `scripts/validate_source_confirmation_brief.py`: validate the confirmation brief against its source rehearsal summary. Use it before showing or reusing a copied brief; it rejects authorization leakage, missing command templates, coverage/count drift, objective completion overclaiming, and summary mismatch.
- `scripts/prepare_source_confirmation_next_step.py`: consume a validated `source-confirmation-brief.json` and emit the next local handoff from its `executionIntake`. Use it after the user confirms content intent or when resuming from a brief: it can prepare the local confirmed-execution command, identify the read-only create-preflight target, or point to the non-executable gated create-site runbook/evidence bundle. It never authorizes or performs remote mutation.
- `scripts/apply_source_confirmation_next_step.py`: apply a `source-confirmation-next-step` handoff only for local stages. Use it when the handoff has `localCommandReady=true` to generate confirmed execution artifacts without parsing shell commands by hand. It intentionally stops at browser boundaries such as read-only create preflight or gated create-site runbook, and never authorizes or performs remote mutation.
- `scripts/apply_create_preflight_to_confirmed_execution.py`: after read-only `/sites` create preflight evidence is collected, consume the prior source-confirmation apply result plus the filled `create-site-preflight.json` and regenerate confirmed execution artifacts with create-site handoff, validation, non-executable runbook, and created-site evidence bundle. Use it to avoid reconstructing the long confirmed-execution command from confirmation text and deferrals. It remains local-only and does not authorize or submit the create-site form.
- `scripts/build_source_site_package.py`: build a local `allincms_source_site_package` JSON from a distilled source wiki. Use it before user confirmation and before any remote create/upload stage.
- `scripts/validate_source_site_package.py`: validate the local source-site package and enforce no remote mutation, source refs, site-info/navigation presence, confirmation gate, draft schema state, and no raw Markdown/credential leakage.
- `scripts/make_source_package_review_packet.py`: create a redacted local review packet from a publication-ready source-site package before asking the user to confirm. It summarizes site info, navigation keys, page/product/post counts, slugs, paths, source refs, operation gaps, blocked actions, and suggested confirmation text without storing long body copy or raw source text.
- `scripts/validate_source_package_review_packet.py`: validate the review packet against the source package and reject sensitive text, full-copy leakage, count drift, missing blocked actions, or language that implies remote mutation authorization.
- `scripts/make_source_review_objective_coverage.py`: summarize a validated source-package review packet into the pre-browser objective checklist. Use it before asking for content-intent confirmation or answering whether source files are ready for site creation/upload. It can return `reviewComplete=true`, but the full objective still has `complete=false` until live site creation/selection, schema capture, sample upload, batch upload, frontend launch, cleanup, and closeout are proven.
- `scripts/make_source_package_confirmation.py`: create the local user-confirmation record for a validated source-site package after the review packet has been shown or summarized. It requires `--review-packet`, confirms content intent only, and must not be treated as remote mutation authorization.
- `scripts/validate_source_package_confirmation.py`: validate the confirmation record against the source package and review packet, including accepted site info/navigation/content fields, confirmed counts, blocked remote actions, review-packet binding, and local-only/no-mutation flags.
- `scripts/prepare_confirmed_site_execution.py`: after the user confirms a review packet, prepare the local execution bundle in one step: confirmation record, execution plan, exported draft artifacts, source execution status, create-site preflight brief when preflight is missing, and create-site handoff plus non-executable browser runbook when fresh create-site preflight is provided. It remains local-only and does not authorize remote actions.
- `scripts/build_confirmed_site_execution_plan.py`: after package confirmation, build a prepared-only execution plan that sequences create/select site, setup inspection, pages/site-info handoff and execution, schema capture, sample upload, batch upload, forms/media/settings, final audit, and cleanup while preserving action-specific authorization boundaries.
- `scripts/make_create_site_preflight_brief.py`: after package confirmation and a `new_site` execution plan, build a non-authorizing read-only browser brief for collecting `/sites` and create-dialog evidence needed by `make_create_preflight_evidence.py`. Use it when confirmed execution reports `needs_create_site_preflight`.
- `scripts/make_created_site_evidence_brief.py`: after a confirmed create-site handoff is ready, build a non-authorizing post-submit evidence brief for collecting the new site key, backend/frontend URLs, module routes, setup-page proof, and one content-type read-only inspection required by `make_created_site_evidence.py`.
- `scripts/build_confirmed_create_site_handoff.py`: after package confirmation, execution-plan creation, and fresh create-site preflight, build a non-authorizing handoff for creating the new site from confirmed package siteName/siteDescription. It prepares suggested authorization text, authorization-record command, pre-mutation gate command, browser steps, and forbidden neighboring actions.
- `scripts/build_create_site_runbook.py`: turn a confirmed create-site handoff into a concrete browser runbook for opening `/sites`, filling the create dialog, submitting once after action-time authorization/gate, and writing created-site evidence. It keeps `browserStepsExecutable=false` and must be followed by real browser evidence, not treated as proof that a site was created.
- `scripts/prepare_create_site_authorization.py`: consume a create-site apply result or runbook and prepare the action-time authorization boundary. Without current user authorization text it writes a non-authorizing package with suggested authorization text and command templates. With exact current user authorization text it writes the local authorization record and validates `check_pre_mutation_gate.py` for `create_site`, while still not submitting the form or making the runbook executable.
- `scripts/prepare_created_site_evidence_bundle.py`: after the create-site runbook and created-site evidence brief exist, prepare a local evidence bundle with a fillable redacted evidence template, notes, an apply command, the lower-level `make_created_site_evidence.py` command file, and `prepare_created_site_schema_capture.py` command file. The template must later record the authorization record and `preMutationGateStatus=passed`; the bundle itself is scaffolding only and does not authorize or prove site creation.
- `scripts/apply_created_site_evidence_bundle.py`: after the gated create-site browser submit, consume the filled created-site evidence bundle template, require a concrete authorization record and passed pre-mutation gate proof, write standard `created-site-evidence.json`, and optionally run `prepare_created_site_schema_capture.py` in the same local-only apply step. Use this instead of hand-copying many template fields into `make_created_site_evidence.py`.
- `scripts/apply_created_site_evidence_to_source_rehearsal.py`: after `apply_create_preflight_to_source_rehearsal.py` and one gated browser create-site submit produced a filled created-site evidence template, consume the source apply result and filled template, reuse the created-site evidence bundle, reload package/review context from confirmation, apply created-site evidence, and prepare schema/pages/site-info/taxonomy artifacts in one local-only step. Use it instead of hand-stitching package, review packet, confirmation, execution plan, artifact readiness, and evidence bundle paths after a source rehearsal.
- `scripts/export_confirmed_site_artifacts.py`: export a confirmed source package into runtime artifacts: products/posts draft manifests, pages/site-info/forms/media/navigation plans, and artifact readiness. Use it after confirmation and before schema capture/upload planning.
- `scripts/prepare_pages_site_info_execution.py`: after confirmed artifacts exist and the created/selected site has current setup-page preflight evidence, convert `pages-plan.json` and `site-info-plan.json` into a local browser handoff for site-info save and page/theme/route actions. It keeps every action non-executable until concrete targets, action-time authorization, and pre-mutation gates exist.
- `scripts/make_theme_page_target_map.py`: after read-only theme page list/design URL inspection, convert `pages-site-info-browser-handoff.json` plus a redacted observation JSON into a concrete theme/page target map. Use it before authorizing `save_design`, `publish_design`, `enable_theme_page`, or `bind_route` so `{themeId}` and `{pageId}` placeholders are replaced by current-site URLs, and missing source pages are separated for `create_theme_page`.
- `scripts/prepare_pages_site_info_evidence_bundle.py`: after a pages/site-info browser handoff exists, prepare a local evidence bundle with a fillable execution-evidence template, notes, validation command, and apply command. It is scaffolding only; browser actions still need per-action authorization/gates and redacted backend/frontend proof.
- `scripts/prepare_default_theme_bootstrap.py`: when a newly created or selected site's public frontend is 404/blank and themes are missing or have zero usable pages, prepare a local default-theme bootstrap runbook. It generates separate `create_theme` and `activate_theme` authorization commands, requires preset `默认`, and writes a redacted evidence template. It does not create or activate a theme.
- `scripts/validate_default_theme_bootstrap_evidence.py`: validate redacted evidence after a default-theme bootstrap run. It requires preset `默认`, concrete `themeId`, generated starter page count, theme activation proof, route checks, public non-empty frontend DOM for required starter paths, and no raw credential/header material. Passing this fixes the blank-theme foundation only; business content, homepage modules, products, posts, forms, media, and launch QA remain separate gates.
- `scripts/apply_default_theme_bootstrap.py`: after default-theme bootstrap evidence validates, apply that foundation proof back into created-site evidence and write `created-site-evidence.after-default-theme-bootstrap.json`. Use the refreshed evidence for later pages/site-info, taxonomy, and schema-capture preparation. When `--prepare-created-site-schema-capture` plus artifact-readiness context is supplied, it also reruns the local created-site preparation from the refreshed evidence and emits the downstream source status/next-stage handoff. It does not create, activate, save, publish, upload, or claim business content complete.
- `scripts/prepare_taxonomy_execution.py`: after `taxonomy-plan.json` exists and the created/selected site has current products/posts read-only evidence, prepare non-executable category/tag create-or-map actions for product and post taxonomy. It does not create or map terms; each term action still needs current UI/request schema, action-time authorization, and pre-mutation gate. Category create is a capturable Server Action that returns a `categoryId` — prefer JSON replay over UI clicking (see `references/server-action-save-api.md` §2), and run taxonomy first: products/posts reference categories as an ID array, so create categories and record each `categoryId` before any product/post payload references them.
- `scripts/validate_taxonomy_execution_evidence.py`: validate redacted browser evidence after taxonomy terms were created or mapped. Use it before relying on categories/tags in products/posts manifests or homepage category modules.
- `scripts/prepare_taxonomy_evidence_bundle.py`: after a taxonomy execution handoff exists, prepare a local evidence bundle with a fillable taxonomy evidence template, notes, validation command, and apply command. It is scaffolding only; browser actions still need per-term authorization/gates and redacted backend mapping proof.
- `scripts/apply_taxonomy_execution.py`: after authorized taxonomy browser work has produced redacted create/map evidence, validate it and refresh source execution status in one local-only step. Use it to advance from `taxonomy_execution` to schema or manifest gates without hand-wiring status arguments.
- `scripts/bind_created_site_to_artifacts.py`: after a new site has `created_verified` evidence, or an existing site has fresh `existing_site_selected` read-only evidence, bind the real `siteKey` and `frontendBaseUrl` into exported draft products/posts manifests and artifact readiness while keeping `schemaVerified: false`. Existing-site binding is continuation proof only, not proof that a new site was created.
- `scripts/prepare_created_site_schema_capture.py`: after created or selected-site evidence exists, run site artifact binding, pages/site-info browser handoff preparation, taxonomy execution handoff preparation, schema-capture handoff, schema-capture progress, and source execution status in one local-only step. It does not create probes, create/map taxonomy, save site-info, create pages, save design, publish, upload, or verify schemas.
- `scripts/apply_pages_site_info_execution.py`: after authorized pages/site-info browser work has produced redacted execution evidence, validate it and refresh source execution status in one local-only step. Use it to advance from `pages_site_info_execution` to taxonomy or schema gates without hand-wiring status arguments.
- `scripts/build_schema_capture_handoff.py`: after created/selected-site artifact binding, prepare the per-content-type schema-capture handoff for bound products/posts draft manifests. It creates no probe and performs no save; it only reports which content types are ready for create-probe authorization and which still need read-only list/edit preflight.
- `scripts/merge_content_type_preflight.py`: after a read-only products/posts refresh, merge that content type's list/edit evidence into created-site evidence so `build_schema_capture_handoff.py` and the create-probe gate can use the correct per-content-type preflight without losing the original site creation proof.
- `scripts/summarize_schema_capture_progress.py`: after schema-capture handoff, create-probe evidence, save handoff/runbook, save-capture evidence, and schema manifests start accumulating, summarize the exact per-content-type state. It reads local artifacts only and prevents jumping from create-probe readiness directly to manifest sample or batch upload.
- `scripts/apply_save_capture_to_manifest.py`: after one content-type save request capture is validated, apply its `fieldMapping` and `payloadTemplate` to that content type's draft manifest and write a schema-verified manifest. This is local schema binding only; it does not upload, publish, or prove sample/frontend success.
- `scripts/prepare_schema_manifest_sample.py`: after validated save-capture evidence exists, generate the schema-verified manifest, manifest upload readiness report, one-item sample upload runbook, optional schema-capture progress, and optional refreshed source execution status. It does not upload or publish the sample.
- `scripts/build_manifest_sample_upload_runbook.py`: after a schema-verified manifest exists, prepare the one-item sample upload/publish runbook for a selected manifest slug. It remains non-executable until action-time authorization and the pre-mutation gate pass.
- `scripts/prepare_manifest_sample_evidence_bundle.py`: after a manifest sample runbook exists, prepare a local evidence bundle with a fillable sample evidence template, notes, validation command, and apply command. It is scaffolding only; browser actions still need sample authorization/gate and redacted backend/frontend proof.
- `scripts/validate_manifest_sample_upload_evidence.py`: validate redacted evidence from one manifest sample upload/publish and frontend detail check. If the sampled manifest item contains `coverImage`, `media`, or `gallery`, the sample evidence must prove `coverOrMediaVerified=true`; note-only absence is allowed only for no-media manifest items. Batch upload remains blocked until this evidence passes for the same manifest/site/content type.
- `scripts/apply_manifest_sample_upload.py`: after one authorized source-generated manifest sample upload has produced redacted evidence, validate it, write a progress entry, and refresh source execution status in one local-only step. Use it to advance from `sample_upload` to `batch_upload` without hand-wiring status arguments.
- `scripts/prepare_batch_upload_publish.py`: after validated manifest sample evidence exists, validate the sample against the schema-verified manifest, seed the batch progress log with `mediaRequired` flags per slug, and build the batch upload/publish runbook plus evidence bundle in one local-only step. It does not upload, publish, or authorize browser execution.
- `scripts/prepare_batch_upload_publish_evidence_bundle.py`: after a batch upload/publish runbook exists, prepare a local evidence bundle with a fillable batch evidence template, progress log target, final-audit commands, validation command, and apply command. It is scaffolding only; browser actions still need batch authorization/gate and redacted per-slug backend/frontend proof.
- `scripts/apply_batch_upload_publish.py`: after an authorized batch upload/publish run has produced redacted evidence, validate it, write a batch progress log, and refresh source execution status in one local-only step. Use it to advance from `batch_upload` to `forms_media_settings`; launch acceptance still needs forms/media/settings proof or explicit deferrals.
- `scripts/validate_forms_media_settings_evidence.py`: validate redacted forms/media/settings proof or explicit deferrals after the browser stage. It rejects missing per-module decisions and raw credentials/request material.
- `scripts/prepare_forms_media_settings_evidence_bundle.py`: when source status is at `forms_media_settings`, generate a local fillable evidence bundle (`forms-media-settings-evidence.filled.json`, notes, validation command, apply command) before browser proof. Use the bundle path with `prepare_source_next_stage.py --forms-media-settings-evidence-bundle` after the filled proof exists.
- `scripts/apply_forms_media_settings.py`: after forms/media/settings proof or explicit deferrals exist, validate the evidence, write `forms-media-settings-validation.json`, and refresh source execution status in one local-only step. Use it to advance from `forms_media_settings` to `launch_acceptance`; final audit, cleanup, and launch acceptance still remain separate gates.
- `scripts/prepare_launch_acceptance_inputs_bundle.py`: when source status is at `launch_acceptance`, generate a local fillable inputs bundle (`launch-acceptance-inputs.filled.json`, notes, validation command, apply command) for final run evidence, module coverage, final frontend audit, cleanup, closeout, and source-context paths. Use it with `prepare_source_next_stage.py --launch-acceptance-inputs-bundle` after final browser proof exists.
- `scripts/apply_launch_acceptance.py`: after launch evidence exists, run the launch acceptance validator, optionally generate final source-run closeout, write `launch-acceptance-validation.json`, refresh source execution status, emit the next-stage handoff, and write final `source-run-acceptance-validation.json` in one local-only step. Use it to advance from `launch_acceptance` to `complete`; invalid launch proof must leave the status blocked at `launch_acceptance` and the source-run acceptance report rejected.
- `scripts/make_source_run_final_closeout.py`: generate the `allincms_source_run_final_closeout` artifact required by launch acceptance and final source-run acceptance. Use it directly for diagnostics or through `apply_launch_acceptance.py --auto-final-closeout` so final closeout proof is not hand-written. It validates source status, launch acceptance, cleanup proof, required artifact paths, proof terms, and sedimentation status; it does not perform browser work.
- `scripts/make_source_objective_coverage.py`: summarize a `validate_source_run_acceptance.py` report into the user's file-to-site objective checklist. Use it for status answers and final closeout summaries so partial browser proof is not mistaken for the full objective. It is summary evidence only; final completion still requires the same acceptance report to have `accepted=true`.
- `scripts/validate_pages_site_info_execution_evidence.py`: validate redacted browser evidence after site-info save and page/theme/route actions have actually run. It derives required page proof from each handoff page's `actions`: existing default-page reuse can omit `create_theme_page`, while save/publish/enable/bind actions still require their own proof plus frontend proof. It rejects placeholder `{themeId}` / `{pageId}` URLs or raw credential/header residue.
- `scripts/summarize_source_execution_status.py`: summarize the source-file execution chain from package, review packet, confirmation, execution plan, artifact export, create-site handoff, created-site artifact binding, pages/site-info handoff, validated pages/site-info execution proof, taxonomy execution, schema-capture handoff, schema manifests, sample evidence, batch proof, forms/media/settings evidence, and launch acceptance. Use it as a local stage dashboard; it never authorizes or performs browser mutations.
- `scripts/prepare_source_next_stage.py`: convert a current `allincms_source_execution_status` JSON into one local next-stage handoff. Use it after every source status refresh to avoid hand-copying context paths or jumping stages. It may emit a real helper command for local apply/preparation stages, including applying post-create source-rehearsal evidence with `--create-preflight-source-apply-result` plus `--filled-created-site-evidence-template`, applying filled created-site evidence bundles with `--created-site-evidence-bundle` and `--filled-created-site-evidence-template`, applying filled pages/site-info evidence bundles with `--pages-site-info-evidence-bundle`, applying filled taxonomy evidence bundles with `--taxonomy-evidence-bundle`, applying filled manifest sample evidence bundles with `--sample-evidence-bundle`, applying filled batch upload evidence bundles with `--batch-evidence-bundle`, applying filled forms/media/settings evidence bundles with `--forms-media-settings-evidence-bundle`, preparing launch-acceptance inputs when no filled bundle exists, expanding filled launch acceptance inputs bundles with `--launch-acceptance-inputs-bundle`, applying default-theme bootstrap evidence when `--default-theme-bootstrap-runbook` and `--default-theme-bootstrap-evidence` are provided, or mark browser-only stages as requiring browser evidence/authorization. It never authorizes, clicks, saves, uploads, publishes, or mutates AllinCMS.
- `scripts/make_source_progress_status.py`: read an existing `source-file-rehearsal-summary.json` or `allincms_source_execution_status` and produce a compact current-progress report with next blocker and batch-readiness status. Use it for mid-run status answers such as "can batch upload now?" or "what is next?" without rebuilding status or claiming final launch completion.
- `scripts/validate_source_run_acceptance.py`: final local acceptance gate for source-file-driven runs. Use it only after launch acceptance has been applied back into source execution status. It requires `currentStage=complete`, all source stages passed, a next-stage handoff pointing to `complete`, package/review/confirmation artifacts, valid launch acceptance, readable source-wiki proof, complete matching `contentGoalCoverage`, directly loadable created-site binding/upload/sample/batch/forms-media-settings/final-audit/cleanup/closeout artifacts, and consistent `siteKey` / frontend origin across those artifacts. It does not replace browser evidence; it prevents overclaiming the full user-file-to-live-site objective from partial or cross-site artifacts.
- `scripts/simulate_manifest_rehearsal.py`: run a local-only posts/products manifest rehearsal that proves a neutral draft manifest passes generic validation, correctly fails the upload schema gate until a real save request provides `payloadTemplate`, and carries a simulated source-input gap ledger into `operationGaps`.
- `scripts/validate_manifest_rehearsal.py`: validate manifest rehearsal artifacts and confirm schema-gate failure plus source gap-ledger merge are expected for draft manifests.
- `scripts/validate_run_evidence.py`: run before claiming an end-to-end simulation is complete.
- `scripts/validate_full_e2e_simulation.py`: run after `simulate_full_e2e_chain.py` to verify the output directory contains coherent site creation, probe lifecycle, and module interface planning artifacts.
- `scripts/summarize_run_status.py`: summarize validated run evidence into proven items, missing items, required completion proof, completion gaps, evidence freshness, and next actions before the final response.
- `scripts/prepare_next_action_authorization.py`: turn `summarize_run_status.py --output` next-action details into a non-authorizing preparation package with suggested user authorization text, authorization-record command template, pre-mutation gate command, and optional expected missing-authorization gate failure. Use it when the summary emits `nextActionDetails`; it does not create user authorization.
- `scripts/make_round_maintenance_summary.py`: create a closeout-compatible summary for documentation-only, helper-script, request-analysis, or validation-only rounds that do not produce a real run-evidence JSON. This is not remote LAICMS proof.
- `scripts/merge_probe_evidence.py`: merge staged probe request-capture, sample-verification, and cleanup proof into run evidence after each separately authorized browser step. Prefer `--save-capture-evidence`, `--publish-sample-evidence`, or `--cleanup-evidence` after the corresponding browser run so redacted proof is validated before merging.
- `scripts/check_round_closeout.py`: run before the final response of every AllinCMS skill turn to verify summary fields, enforce that skill changes are reported as sedimentation, require at least one `--round-issue` observation, and require no-change turns to explicitly say `no reusable skill update needed`.
- `scripts/audit_frontend_rendering.py`: run against frontend URLs after sample upload and after batch publish to catch raw Markdown residue, missing structure, bad status, and image issues.
- `scripts/make_launch_audit_inputs.py`: generate URL and expected-status files for mixed static/detail launch audits.
- `scripts/make_final_frontend_audit_inputs.py`: generate final frontend audit URL and expected-status files from a schema-verified posts/products manifest, optionally requiring a complete batch progress log before final audit.
- `scripts/make_final_frontend_audit_stage_result.py`: convert a redacted final frontend audit report into a `final_frontend_audit` browser-stage result so the execution ledger can be advanced without hand-written proof.
- `scripts/make_frontend_rendering_evidence.py`: convert redacted frontend audit JSON into a `frontendRendering` run-evidence block.
- `scripts/make_launch_readiness_evidence.py`: generate a `launchReadiness` block after backend theme/page/route state and public HTTP/DOM launch checks are verified. Merge it into run evidence only when all readiness booleans are true; partial output with blockers is diagnostic evidence and the run-evidence validator intentionally rejects it as launch-ready proof.
- `scripts/summarize_module_scan.py`: compress a redacted read-only backend module scan into interface evidence and flag that `_rsc` fetches may be prefetches, not mutation APIs.
- `scripts/plan_module_capture.py`: turn `summarize_module_scan.py` output into a staged browser-capture plan. It groups visible controls by authorization boundary and stop condition; it does not grant permission to mutate LAICMS.
- `scripts/validate_capture_plan_gate_coverage.py`: run after `plan_module_capture.py` and before browser capture authorization. It verifies every capture-plan `authorizationAction` exists in the authorization helper, has a field template, and either has a pre-mutation gate or is explicitly allowlisted as ungated.
- `scripts/validate_action_replay_contract.py`: validate a redacted per-action JSON/Server Action replay contract after a real UI request, sample replay, backend persistence proof, and public frontend proof when applicable. Use this before marking any captured action as replay-ready; module capture coverage alone is not enough.
- `scripts/apply_action_replay_contracts.py`: apply a complete set of validated per-action replay contracts to a local module-capture coverage file. It turns `jsonReplayReady` on only when every captured coverage stage has a matching valid contract; it does not authorize replay or mutate LAICMS.
- `scripts/prepare_capture_authorization.py`: prepare suggested authorization text plus local authorization-record and pre-mutation-gate commands for one capture-plan stage. It does not replace the user's action-time authorization.
- `scripts/validate_capture_authorization_package.py`: validate a prepared capture-plan authorization package before asking for or acting on authorization. It checks package/plan alignment, concrete target URL when commands are emitted, one module/action only, non-authorizing warning, command/gate action alignment, and simulated/template command suppression.
- `scripts/prepare_all_capture_authorizations.py`: prepare and validate local authorization packages for every stage in a capture plan. Use it after a real module scan and capture plan when you need a complete per-action checklist. It only writes local JSON package files and a `summary.json`; it does not authorize, click, save, upload, publish, delete, or mark any action JSON-replay-ready.
- `scripts/validate_all_capture_authorizations.py`: validate a capture authorization package-set summary after generation or before reuse. It reopens each referenced package file, validates it against the capture plan, checks summary/package consistency, and rejects stale, missing, duplicated, or drifted package entries.
- `scripts/make_capture_handoff.py`: validate a full local E2E output directory and generate the next single-stage capture handoff. For local-only simulation output, command fields are suppressed and simulated targets are templated by default so they cannot be mistaken for real LAICMS authorization.
- `scripts/validate_capture_handoff.py`: validate that a generated capture handoff is safe, especially that local-only handoffs suppress commands and do not expose simulated targets in user-facing authorization text.
- `scripts/build_launch_plan.py`: build a local-only launch proof plan from a full rehearsal. It lists static/detail route expected statuses, launch proof gates, module capture readiness, and neutral command templates without storing business copy or concrete slugs.
- `scripts/validate_launch_plan.py`: validate that a launch proof plan is local-only, uses redacted route patterns, contains required proof gates, and does not leak simulated site keys into user-facing fields.
- `scripts/validate_launch_acceptance.py`: validate from-scratch launch acceptance evidence before claiming a website is complete. Use it after real run evidence, module coverage, schema-gated upload readiness, batch proof, forms/media/settings proof or explicit deferral, final frontend audit, cleanup, and round closeout exist. Local rehearsal or stage coverage alone must fail this gate.
- `scripts/build_browser_execution_plan.py`: build a local-only staged real-browser execution plan from a full rehearsal. It sequences read-only refresh, site creation, setup inspection, module capture, theme/page/route launch, content probe, schema gate, batch upload, forms/media/settings, final audit, and cleanup with per-stage authorization and stop conditions.
- `scripts/validate_browser_execution_plan.py`: validate that the staged browser execution plan is template-only, local-only, contains required stages, separates read-only/verification/mutation modes, and does not leak simulated site keys into user-facing fields.
- `scripts/build_browser_execution_ledger.py`: build a local-only execution ledger from the staged browser plan. It marks the next safe stage, blocks dependency jumps, and provides per-stage evidence slots so real browser progress can be recorded without widening authorization.
- `scripts/validate_browser_execution_ledger.py`: validate that the execution ledger matches the required stage sequence, has coherent counts, exposes only one ready next stage when dependencies are incomplete, and does not leak simulated site keys into user-facing fields.
- `scripts/branch_existing_site_ledger.py`: after `refresh_readonly_site_evidence` is completed and fresh `existing_site_selected` run evidence is validated, branch a from-scratch ledger into an existing-site continuation by skipping `create_site_submit` and making `setup_pages_inspection` the next ready stage. This is local ledger bookkeeping only and must not be used as proof that a new site was created.
- `scripts/build_browser_stage_packet.py`: build the single next-stage execution packet from the ledger. It packages the target template, allowed actions, required proof, stop condition, optional authorization text template, evidence-capture template, and ledger-update command for exactly one ready stage.
- `scripts/validate_browser_stage_packet.py`: validate that the next-stage packet targets a ready ledger stage, is local-only, contains no simulated site key, and cannot be mistaken for broad browser authorization.
- `scripts/prepare_browser_stage_evidence_bundle.py`: create a local per-stage evidence directory from a validated browser-stage packet before browser execution. It writes an evidence manifest, a partial stage-result template, and notes for required proof without granting authorization or touching LAICMS.
- `scripts/validate_browser_stage_evidence_bundle.py`: validate a per-stage evidence bundle before real browser execution or ledger apply. It checks manifest/packet alignment, the partial stage-result template, notes proof rules, redaction, and local-only/non-mutating status.
- `scripts/prepare_browser_stage_authorization.py`: prepare local authorization-record and pre-mutation-gate command templates for a ready browser-stage packet when the stage is supported. Currently supports `create_site_submit`, `module_interface_capture`, granular `theme_page_route_launch` sub-actions, content probe lifecycle stages (`content_probe_create`, `save_request_capture`, `publish_sample_verify`, `cleanup_probes`), `batch_upload_publish`, and granular `forms_media_settings` sub-actions. The module capture case selects exactly one capture-plan `module:action` from the coverage file and must not authorize the aggregate stage broadly. The theme/page/route launch case requires `--launch-action` and prepares exactly one site action such as `create_theme_page`, `save_design`, `publish_design`, `enable_theme_page`, `set_homepage`, `create_route`, or `bind_route`; do not authorize the aggregate launch stage broadly. The forms/media/settings case requires `--settings-action` and prepares exactly one action such as `save_site_settings`, `create_theme`, `create_form`, `add_domain`, or `add_tracking_tag`; do not authorize the aggregate settings/media/forms stage broadly. The generic helper still does not emit an `upload_media` command; it must not fabricate one. Actual AllinCMS media execution is owned by the canonical adapter and its exact-site runtime check, private index, strict-serial controller, delayed reconciliation, and bounded current-image retry. UI file selection is only an explicitly approved fallback after canonical contract drift or runtime failure. Content probe lifecycle and batch stages require `--content-type`; do not infer content type from `{contentType}` templates or chain create/save/publish/upload/cleanup actions. Unsupported stages must report `gateSupported: false` instead of emitting pretend commands.
- `scripts/validate_browser_stage_authorization_package.py`: validate a prepared browser-stage authorization package before asking for or acting on user authorization. It checks package/packet/preflight alignment, optional capture-plan alignment for `module_interface_capture`, the non-authorizing warning, retained current-user authorization placeholder, target, action, fields, and pre-mutation gate command. Use it after `prepare_browser_stage_authorization.py`; passing validation still does not authorize any browser mutation.
- `scripts/validate_browser_surface_readiness.py`: validate a redacted browser observation before design/theme/page mutation preparation. It blocks Chrome `/sign-in`, wrong-site workspace URLs, zero-size design preview frames, `Render canvas...` stalls, disabled save/publish controls, and explicit blocking issues. It is local-only and does not authorize mutations.
- `scripts/make_browser_stage_authorization_readiness.py`: build queue-ready readiness JSON directly from a validated browser-stage authorization package plus packet, preflight, and optional capture plan. Use it when you need `make_browser_stage_queue.py` input from a browser-stage package without hand-copying target/action/freshness fields. It remains local preparation only and does not create authorization.
- `scripts/make_next_browser_action_handoff.py`: build one validated handoff JSON for the next browser action from a prepared browser-stage authorization package, packet, preflight, and optional capture plan. Use it as the final local bundle before asking the user for action-time authorization. It does not authorize or execute the action.
- `scripts/validate_next_browser_action_handoff.py`: revalidate a generated next-browser-action handoff before asking for authorization or resuming from a handoff. It reopens `sourceFiles`, reruns the browser-stage package validator, checks that handoff fields still match the source package, and rejects any handoff that no longer preserves `preparedOnly: true`, `isUserAuthorization: false`, `remoteMutationsPerformed: false`, and the current-user authorization placeholder.
- `scripts/make_handoff_readiness.py`: build a machine-readable readiness report for one next-browser-action handoff before asking the user for authorization. It validates the handoff, checks preflight freshness, and reports `ready_to_request_authorization` or `blocked_refresh_readonly_evidence` without creating authorization or touching LAICMS.
- `scripts/make_browser_stage_queue.py`: build a local staged authorization queue from the current handoff readiness and manifest upload readiness. Use it to show which browser stage is ready to request authorization and which later stages remain locked behind proof. It writes local JSON only; it does not authorize, click, save, publish, upload, delete, or batch-submit.
- `scripts/make_browser_stage_queue_from_summary.py`: build the same local staged authorization queue directly from a valid `summarize_run_status.py --output` file when `nextActionDetails` already contains the supported next action but no next-browser-action handoff/readiness file exists yet. Use it for real-site continuation after a run summary emits `create_product_probe`.
- `scripts/make_e2e_gap_audit.py`: build a local completion-gap audit from current read-only evidence, upload-readiness, and browser-stage queue. Use it before reporting progress on a broad site-build/content-upload objective so proven phase evidence is not overstated as complete.
- `scripts/prepare_probe_save_handoff.py`: after an authorized create-probe stage produced a draft edit URL, build the next non-authorizing `save_probe` handoff from redacted create evidence and fresh preflight evidence. Use it to avoid hand-writing save/capture authorization text, command templates, and gate commands.
- `scripts/prepare_schema_save_capture.py`: after create-probe evidence exists for one content type, build the save-probe handoff, save-capture browser runbook, and refreshed schema-capture progress in one local-only step. It does not click save or capture a request.
- `scripts/prepare_existing_probe_save_handoff.py`: when a read-only resume finds an existing Codex Probe edit page and the goal is to capture non-empty body/editor schema, prepare a non-authorizing `save_probe` handoff from backend read-only state and a concrete edit URL. Use it only as preparation; it still requires action-time save/capture authorization and the normal `save_probe` gate before saving.
- `scripts/build_probe_save_runbook.py`: turn a save-probe handoff into a local browser execution runbook with field edits, network-capture points, persistence checks, forbidden actions, and a redacted evidence template. It supports both the normal create-to-save handoff and the existing-probe resume handoff; for existing probes, the runbook should capture a non-empty body/editor sample without renaming the item or requiring draft status. It is preparation only and must not be executed until the `save_probe` authorization record exists and the pre-mutation gate passes.
- `scripts/validate_probe_save_runbook.py`: validate a save-probe runbook, source handoff, preflight, optional authorization record, and save gate before browser execution. Use `--expect-missing-authorization` during local preparation to prove the runbook is safely blocked; use `--fail-on-blocked` before touching the browser after an authorization record is created.
- `scripts/prepare_probe_save_evidence_bundle.py`: prepare a local save-capture evidence folder from a validated save-probe runbook. It writes an unfilled template, a separate `save-capture-evidence.filled.json` target, capture notes, and a validation command that binds to base run evidence only when the source handoff exposes a preflight/run-evidence file. It is scaffolding only; do not validate or merge the unfilled template.
- `scripts/validate_probe_save_capture_evidence.py`: validate the redacted evidence captured after one authorized save-probe run. It rejects raw cookies, authorization values, server-action values, router-state blobs, account emails, incomplete field mappings, missing payload templates, unverified backend persistence, publish leakage, or non-current-site URLs before evidence is merged.
- `scripts/build_probe_publish_runbook.py`: after request-capture proof is merged and the summary emits `authorize_publish_probe`, build a local browser runbook for publishing the probe once and verifying backend/frontend sample state. It is preparation only and must not be executed until the `publish_probe` authorization record exists and the pre-mutation gate passes.
- `scripts/validate_probe_publish_sample_evidence.py`: validate the redacted evidence captured after one authorized publish-probe run. It rejects raw credentials, cross-site frontend URLs, missing backend/frontend proof, missing render audit, and publish evidence that is not tied to the same base run evidence.
- `scripts/build_probe_cleanup_runbook.py`: after sample backend/frontend proof is merged and the summary emits `authorize_cleanup_probe`, build a local browser runbook for deleting or unpublishing only the probe. It is preparation only and must not be executed until the `cleanup_probe` authorization record exists and the pre-mutation gate passes.
- `scripts/prepare_existing_probe_cleanup_handoff.py`: when a read-only resume finds an existing public Codex Probe but older artifacts lack the full requestCapture/sampleVerification chain, prepare a non-authorizing cleanup handoff from current backend read-only state, frontend audit, concrete edit URL, and current preflight. Use this to avoid creating duplicate probes while still requiring action-time cleanup authorization and the normal `cleanup_probe` gate.
- `scripts/build_existing_probe_cleanup_runbook.py`: turn a valid existing-probe cleanup handoff into a local browser runbook with concrete browser steps and a redacted evidence template. It remains `browserStepsExecutable: false` until the `cleanup_probe` authorization record exists and `check_pre_mutation_gate.py --action cleanup_probe` passes.
- `scripts/validate_probe_cleanup_evidence.py`: validate the redacted evidence captured after one authorized cleanup-probe run. It rejects non-probe cleanup candidates, cross-site backend URLs, missing backend/frontend non-public proof, count mismatches, raw credential leakage, or cleanup evidence that is not tied to the same base run evidence.
- `scripts/build_theme_page_create_runbook.py`: after a `create_theme_page` browser-stage authorization package is prepared, build a local browser runbook for creating exactly one dynamic theme child page such as `/products/{product}`. It remains non-executable until action-time authorization and the pre-mutation gate pass.
- `scripts/validate_theme_page_create_evidence.py`: validate redacted evidence after an authorized `create_theme_page` run. It requires request capture, dynamic `routePath`, `pageId`, backend row proof, stop-condition proof, and no raw credentials before later design, publish, enable, bind, sample verification, or batch upload can rely on the page.
- `scripts/build_batch_upload_publish_runbook.py`: after request capture, sample verification, and a schema-verified posts/products manifest exist, build a local browser runbook for the batch upload/publish stage. It retains the authorization placeholder, names forbidden neighboring actions, and starts with `browserStepsExecutable: false`.
- `scripts/validate_batch_upload_publish_evidence.py`: validate redacted batch evidence after an authorized batch stage. It binds manifest, base run evidence, progress log, backend/frontend URLs, body/cover checks, manifest media requirements, and optional redacted frontend audit JSON before later stages can rely on the batch proof.
- `scripts/make_browser_stage_result.py`: generate a redacted `allincms_browser_stage_result` JSON from the packet after a browser stage. Use this instead of hand-writing result JSON; completed results inherit the packet's required proof, while partial/blocked results must name captured proof and blockers explicitly.
- `scripts/apply_browser_stage_result.py`: validate a redacted result for one browser stage and apply it to the execution ledger. Use after each real or simulated stage to mark the stage completed, blocked, or partial, record proof pointers, and compute the next ready stage.
- `scripts/validate_browser_stage_result.py`: validate one browser stage result against the stage packet, including required proof coverage and redaction constraints.
- `scripts/update_module_capture_coverage.py`: update the local module/action capture coverage ledger after each `module_interface_capture` result, and optionally sync that coverage back into the browser execution ledger. Use it to record which capture-plan stages are captured, pending, blocked, or not applicable; one captured stage must keep `complete: false` and `jsonReplayReady: false`. Complete interface coverage may unlock dependent stages, but `jsonReplayReady` turns true only after `apply_action_replay_contracts.py` verifies every captured stage has a matching per-action replay contract. Ledger sync must still state that fresh action-time authorization is required before replay.
- `scripts/run_full_rehearsal.py`: run the preferred one-command local-only rehearsal: full E2E simulation, full directory validation, next capture handoff generation, browser execution planning, ledger/packet validation, module-capture browser-stage authorization package generation, next-browser-action handoff validation, and real-browser runbook summary generation. Use this before a real browser capture when the goal spans site creation, module interface planning, and content upload.
- `scripts/validate_full_rehearsal.py`: validate the top-level rehearsal summary by rechecking the full E2E directory, capture-plan gate coverage, handoff safety, launch plan safety, next-browser-action handoff safety, artifact paths, selected stage, and launch proof counts.
- `scripts/summarize_rehearsal_stage_coverage.py`: compress a full rehearsal into a 14-stage coverage summary with authorization stage count, remote mutation expectations, partial/recovery coverage, next real browser step, source-input blockers, and final ledger exhaustion. Use it for fast status reporting; it proves local workflow coverage only, not live LAICMS launch completion.
- `scripts/make_browser_runbook_summary.py`: compress a validated rehearsal summary into the next real-browser runbook step, required real evidence, suppressed-command status, selected capture stage, operator handoff checklist, packet/ledger/evidence paths, stage-result paths, ledger apply command, authorization preparation template, and stop conditions. `run_full_rehearsal.py` writes this automatically; use this helper directly only for external or older rehearsal summaries.
- `scripts/validate_browser_runbook_summary.py`: validate a standalone `browser-runbook-summary.json` by reopening its `sourceSummary`, rerunning full rehearsal validation, and checking packet, ledger, evidence bundle, next-browser-action handoff, and authorization-package paths. Run it before reusing a runbook copied across sessions or transferred outside the full rehearsal directory.
- `scripts/audit_skill_hygiene.py`: run after updating this skill to catch business-domain leakage, account emails, and raw content IDs.
- `scripts/content_goal_coverage_utils.py`: shared helper module (imported, not run directly) for carrying source content-goal coverage through the upload stages; the single shared utility module underneath the coverage/objective scripts.
- `scripts/apply_selected_site_to_source_rehearsal.py`: apply selected existing-site read-only evidence into a source-file rehearsal, for the existing-site continuation path (vs the new-site create path).
- `scripts/audit_test_entrypoints.py`: audit that every `test_*.py` runs all its no-arg test functions under a `__main__` guard, so script-style regression tests cannot silently skip. Run alongside `audit_skill_hygiene.py`.
- `scripts/run_authorization.py`: build/validate a run-scoped authorization (one user grant, bound to a siteKey + confirmed package hash, allowlisting only in-scope content-build actions) so `check_pre_mutation_gate.py --run-authorization` can derive per-action authorizations without re-prompting; carve-outs (new site, delete, outward settings, other sites, non-allowlisted actions) always need explicit authorization.
