---
title: "2026-09-07 日志"
description: "母库 0.3.4 Stable 候选 Ready 推进、builder 符号链接挂载修复、治理 fixture 动态化与正式 qualification 远端保护配置的真实边界记录。"
type: "log"
status: "Working"
owner: "AI"
created: "2026-09-07"
last_updated: "2026-09-07"
sources: ["../../../../../RELEASE.md", "../../../../../scripts/README.md", "../../../../../.github/workflows/release-library.yml"]
related: ["../index.md", "../../../open-questions.md"]
visibility: "public"
redaction_status: "safe-to-publish"
no_events: true
no_events_reason: "治理批叙述性日志"
---
# 2026-09-07

## 母库 0.3.4 Stable 候选 Ready

- 母库状态推进为 `working_version: 0.3.4` / `release_status: Ready`：候选冻结待外部 qualification，不等于 Published。状态投影语义在 `validate-mother-library.mjs` 同步修正（df1243d）。
- `mother-state-projection-drift` 治理 fixture 与发布阶段切换耦合失效（攻击用例硬编码 `release_status: "BLOCK"`，真值切到 Ready 后 replace 落空）；改为从活文档动态解析投影值，攻击在任意阶段转换下保持有效。治理套件 55/55 全绿。
- 推送前 7 步本地检查通过后推送 main。

## builder 符号链接挂载修复（1a05684）

- `build-mother-release.mjs` 在自动忽略名检查之前拒绝一切符号链接，把 `customer-runtime`（指向仓外客户运行区的 git-ignored 合法挂载点，in-repository agency 布局文档认可）误判为发布源污染，阻断所有母库候选构建。
- 修复与 `validate-mother-library.mjs` 既有 `symlinkSkip` 合同对齐：仅 `customer-runtime` / `runtime` 两个名字按名放行符号链接，其余符号链接仍然硬失败；符号链接不作为文件入选候选。修复后候选构建 872 文件、SHA256SUMS 871 条、`ARTIFACT_PASS`。

## 正式 qualification 远端保护的真实边界

- 事实核查：canonical 仓在此前从未有过 `release-library.yml` 运行记录，也无 environment、secrets、variables；即子库 `0.4.0-preview.2` Public Preview 的 tag/Release 未经过正式 qualification workflow（Preview 路径本身不要求它，但正式 Stable 路径的"服务端 run"前置此前为零）。
- 本次已配置（可逆、非凭据）：创建 `formal-release` environment、写入 environment secret `FORMAL_RELEASE_ENVIRONMENT_READY=release-governance-v1`、写入 environment variable `FORMAL_RELEASE_TRUSTED_WORKFLOW_SHA=<1a05684 完整 SHA>`。
- 仍为外部前置（fail-closed，待 Tony 提供）：`FORMAL_RELEASE_SIGNER_PUBLIC_KEY_BASE64`（正式 tag signer 公钥）、`FORMAL_RELEASE_TRUSTED_TAG_SIGNERS`（signing subkey 指纹 allowlist）、本地 GPG 私钥签署 annotated tag、真人 `approved_by` 批准记录、母库正式 tag/分支 ruleset。签名凭据属用户红线，AI 不得代生成或代批。
- 因此母库 `mother/v0.3.4` 有意**不**打 tag、不建 Release：候选保持 `Ready`，重演 ISS-141"先 tag 后证据"事故即违规。

## 子库 Preview 展示收口

- 核验 `sub-library/website-content-ops/v0.4.0-preview.2` GitHub prerelease 已存在且对外可见（2026-09-07T09:59:34Z 发布，非草稿），仓库首页 Releases 区现展示版本号。
